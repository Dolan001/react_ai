# Post-write

Check changed paths and generated structure; run the smallest affected frontend checks
and tests; store concise evidence and release the implementer lease.
Reject vague non-PRD feature names, generic components/hooks, business-heavy route files, and files
beyond the structure contract's split threshold.

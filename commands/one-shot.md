# One-shot frontend creation

Create `apps/frontend/` inside the target monorepo after requirements, design, and API
boundaries are approved. Use `rules/project-structure.md` for ownership and
`rules/project-structure.json` for deterministic path validation. Generate
requirement-backed vertical slices; do not copy a prebuilt application tree.

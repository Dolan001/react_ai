# Verify

Run project-owned format, lint, strict TypeScript, unit, production build,
desktop/mobile/tablet E2E, accessibility, responsive, visual, dependency, secret, and
configured container checks. Verification must be independent.
